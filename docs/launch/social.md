# Social posts

Post from your own account. No paid boosts, no engagement pods, and don't tag big accounts to fish for reposts. Two posts, spaced out: #1 on D1 and #2 a few days later, each with a different GIF. The French versions are for your own French-speaking followers. Pick one language per network, or post both if your audience is mixed.

Repo link: `https://github.com/jesuisban22-code/spicetify-terminal`

Limits: X 280 chars (links count as 23), Bluesky 300 chars, Mastodon 500 chars (most instances).

---

## Post #1: boot sequence (attach `screenshots/boot.gif`)

### X / Twitter (EN)
```
Made a Spotify theme that boots like a terminal 🖥️

Terminal for #Spicetify:
› Ctrl+Shift+K command palette (neofetch, cowsay…)
› live ASCII album covers
› visualizer driven by the real track analysis
› matrix rain when idle

https://github.com/jesuisban22-code/spicetify-terminal
#Spotify #unixporn
```

### X / Twitter (FR)
```
J'ai fait un thème Spotify qui démarre comme un terminal 🖥️

Terminal pour #Spicetify :
› palette de commandes Ctrl+Maj+K (neofetch, cowsay…)
› pochettes en ASCII en direct
› visualiseur piloté par l'analyse réelle du morceau
› pluie matrix au repos

https://github.com/jesuisban22-code/spicetify-terminal
```

### Mastodon (EN)
```
I made **Terminal**, a hacker/terminal theme for Spotify via Spicetify.

• boot sequence on launch
• Ctrl+Shift+K command palette, a small shell: play/next/goto/search + neofetch, cowsay, matrix…
• album covers re-rendered live as block-character art
• Now Playing visualizer driven by the track's real audio analysis
• matrix-rain screensaver when paused & idle, CRT scanlines, vim keys
• everything toggleable, respects reduced-motion

Works on Linux & Windows, installable from the Spicetify Marketplace.
https://github.com/jesuisban22-code/spicetify-terminal

#Spicetify #Spotify #Linux #Ricing #unixporn #OpenSource
```
Alt text for the GIF: `Spotify starting with a green-on-black terminal boot log, then revealing the themed home page with album covers drawn as block characters.`

### Mastodon (FR)
```
J'ai fait **Terminal**, un thème hacker/terminal pour Spotify via Spicetify.

• séquence de démarrage au lancement
• palette de commandes Ctrl+Maj+K, un mini-shell : play/next/goto/search + neofetch, cowsay, matrix…
• pochettes redessinées en direct en caractères blocs
• visualiseur piloté par l'analyse audio réelle du morceau
• pluie matrix quand la lecture est en pause, lignes CRT, navigation vim
• tout est désactivable, respecte « réduire les animations »

Linux et Windows, installable depuis le Marketplace Spicetify.
https://github.com/jesuisban22-code/spicetify-terminal

#Spicetify #Spotify #Linux #Ricing #LogicielLibre
```
Texte alternatif : `Spotify démarre avec un journal de boot vert sur noir façon terminal, puis affiche l'accueil du thème avec les pochettes dessinées en caractères blocs.`

### Bluesky (EN)
```
Made a Spotify theme that boots like a terminal 🖥️

Terminal (Spicetify): shell-like command palette, live ASCII covers, a visualizer driven by the real track analysis, matrix rain when idle.

github.com/jesuisban22-code/spicetify-terminal

#Spicetify #unixporn
```

### Bluesky (FR)
```
Un thème Spotify qui démarre comme un terminal 🖥️

Terminal (Spicetify) : palette de commandes façon shell, pochettes ASCII en direct, visualiseur piloté par l'analyse réelle du morceau, pluie matrix au repos.

github.com/jesuisban22-code/spicetify-terminal

#Spicetify
```

---

## Post #2: matrix rain + palette (attach `screenshots/matrix.gif`, or `command-palette.png` + `matrix-rain.png`)

### X / Twitter (EN)
```
Pause Spotify, walk away, come back to this. 🟩

Matrix-rain screensaver + a real command palette (`neofetch`, `cowsay`, `history`, `stats`) in my Spicetify theme "Terminal".

https://github.com/jesuisban22-code/spicetify-terminal
#Spicetify #Spotify
```

### X / Twitter (FR)
```
Mets Spotify en pause, reviens plus tard : 🟩

Économiseur d'écran pluie matrix + vraie palette de commandes (neofetch, cowsay, history, stats) dans mon thème Spicetify « Terminal ».

https://github.com/jesuisban22-code/spicetify-terminal
#Spicetify
```

### Mastodon (EN)
```
Two small things I like in my Spicetify theme "Terminal":

1. Pause the music and go idle → matrix rain takes over (delay and speed configurable, off with reduced-motion).
2. Ctrl+Shift+K opens a command palette that behaves like a tiny shell: `goto library`, `search <query>`, `history`, `stats`, `neofetch`, `cowsay moo`…

https://github.com/jesuisban22-code/spicetify-terminal
#Spicetify #Spotify #Linux #Ricing
```

### Mastodon (FR)
```
Deux détails que j'aime bien dans mon thème Spicetify « Terminal » :

1. Musique en pause + inactivité → la pluie matrix prend le relais (délai et vitesse réglables, désactivée avec « réduire les animations »).
2. Ctrl+Maj+K ouvre une palette qui se comporte comme un mini-shell : `goto library`, `search <requête>`, `history`, `stats`, `neofetch`, `cowsay meuh`…

https://github.com/jesuisban22-code/spicetify-terminal
#Spicetify #Spotify #Linux #Ricing
```

### Bluesky (EN)
```
Pause Spotify, go idle → matrix rain. 🟩
Ctrl+Shift+K → a tiny shell (neofetch, cowsay, history, stats).

My Spicetify theme "Terminal":
github.com/jesuisban22-code/spicetify-terminal
```

### Bluesky (FR)
```
Spotify en pause + inactivité → pluie matrix. 🟩
Ctrl+Maj+K → un mini-shell (neofetch, cowsay, history, stats).

Mon thème Spicetify « Terminal » :
github.com/jesuisban22-code/spicetify-terminal
```

Note: on X, keep each post to at most 2–3 hashtags. More than that reads as spam.
